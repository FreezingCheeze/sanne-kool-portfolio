---
import { Image } from 'astro:assets';
import BaseLayout from '../layouts/BaseLayout.astro';
import ButtonLink from '../components/ButtonLink.astro';
import { site } from '../data/site';
import portrait from '../assets/portrait/sanne-kool.jpg';
---

<BaseLayout>
	<section class="hero">
		<figure class="portrait">
			<Image
				src={portrait}
				alt="Portret van Sanne Kool"
				widths={[320, 480, 760, 1100]}
				sizes="(max-width: 48rem) 80vw, 42vw"
				loading="eager"
				fetchpriority="high"
			/>
		</figure>
		<div class="intro">
			<h1>Mijn naam is Sanne</h1>
			<p>
				Ik ben een enthousiaste grafisch vormgever, in 2025 afgestudeerd aan het ROC Midden Nederland.
				Tijdens mijn opleiding heb ik met veel plezier mijn creativiteit ontwikkeld en mijn liefde voor
				visuele communicatie ontdekt. Ik haal energie uit het vertalen van ideeën naar een sterke en
				herkenbare vormgeving.
			</p>
			<p>
				Op dit moment volg ik de opleiding Eventmanager aan de Hogeschool Utrecht (AD-vorm). Hiermee
				combineer ik mijn passie voor design met de dynamische wereld van evenementen. Ik geloof dat een
				goed ontworpen beleving begint bij een sterk visueel concept!
			</p>
			<div class="actions">
				<ButtonLink href="/opleiding-mediavormgeving/">Opleiding Mediavormgeving</ButtonLink>
				<ButtonLink href="/vrijwerk/">Vrijwerk</ButtonLink>
				<ButtonLink href={site.cvPath} download>CV</ButtonLink>
			</div>
		</div>
	</section>
</BaseLayout>

<style>
	.hero {
		--home-inner-radius: clamp(2.5rem, 6vw, 4.5rem);
		flex: 1;
		display: grid;
		align-items: stretch;
		width: 100%;
		min-height: calc(100dvh - var(--header-height));
	}

	.portrait {
		margin: 0;
		background: transparent;
		display: grid;
		place-items: center;
		padding: clamp(1.5rem, 5vw, 4rem);
	}

	.portrait :global(img) {
		width: min(100%, 28rem);
		aspect-ratio: 4 / 5;
		object-fit: cover;
		object-position: 50% 18%;
		border-radius: var(--radius-lg);
		box-shadow: var(--shadow-soft);
	}

	.intro {
		position: relative;
		background: var(--color-panel);
		color: var(--color-text-on-dark);
		padding: clamp(1.75rem, 5vw, 4.5rem) clamp(1.5rem, 5vw, 4rem);
		border-bottom-left-radius: var(--home-inner-radius);
		display: grid;
		align-content: center;
		justify-items: start;
		text-align: left;
	}
	h1 {
		margin-top: 0;
		max-width: 40rem;
		width: 100%;
		font-size: var(--font-size-2xl);
		color: var(--color-text-on-dark);
	}

	.intro p {
		max-width: 40rem;
		color: var(--color-text-on-dark);
	}

	.intro p:last-of-type {
		margin-bottom: 0;
	}

	.actions {
		display: flex;
		flex-wrap: wrap;
		align-items: stretch;
		justify-content: center;
		gap: var(--space-md);
		margin: var(--space-3xl) 0 0;
		width: min(100%, 40rem);
	}

	.actions :global(.button-link) {
		flex: 1 1 12rem;
	}

	@media (min-width: 64rem) {
		.hero {
			grid-template-columns: minmax(16rem, 1fr) minmax(0, 1fr);
			min-height: 0;
		}

		.intro::before {
			content: '';
			position: absolute;
			top: 0;
			left: 0;
			width: var(--home-inner-radius);
			height: var(--home-inner-radius);
			transform: translateX(-100%);
			background: radial-gradient(
				circle at 0 100%,
				transparent calc(var(--home-inner-radius) - 0.5px),
				var(--color-panel) calc(var(--home-inner-radius) - 0.5px)
			);
			pointer-events: none;
		}
	}
</style>
