---
import SocialLinks from './SocialLinks.astro';
import { site } from '../data/site';
---

<footer class="site-footer">
	<div class="inner">
		<p class="brand">{site.brand}</p>
		<SocialLinks />
		<p class="copy">© {new Date().getFullYear()} door Sanne Kool.</p>
	</div>
</footer>

<style>
	.site-footer {
		margin-top: auto;
		border-top: 0;
		padding-block: var(--space-2xl);
		background: var(--color-bg);
		color: var(--color-text-on-dark);
	}

	:global(body.is-home) .site-footer {
		background: var(--color-bg-muted);
		color: var(--color-text);
	}

	.inner {
		width: min(var(--content-width-wide), 100% - var(--page-gutter) * 2);
		margin-inline: auto;
		display: grid;
		gap: var(--space-lg);
		align-items: center;
	}

	.brand {
		margin: 0;
		font-size: var(--font-size-sm);
		letter-spacing: var(--letter-spacing-wide);
		font-weight: var(--font-weight-medium);
	}

	.copy {
		margin: 0;
		color: var(--color-accent);
		font-size: var(--font-size-base);
	}

	:global(body.is-home) .copy {
		color: var(--color-text-muted);
	}

	.inner :global(.socials a:hover) {
		opacity: 0.85;
	}

	@media (min-width: 48rem) {
		.inner {
			grid-template-columns: 1fr auto 1fr;
		}

		.copy {
			justify-self: end;
		}
	}
</style>
